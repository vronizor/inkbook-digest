services:
  inkbook-digest:
    build: .
    container_name: inkbook-digest
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
