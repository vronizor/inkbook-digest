<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"
      xmlns:dc="http://purl.org/dc/terms/"
      xmlns:opds="http://opds-spec.org/2010/catalog">
  <id>{{ feed_id }}</id>
  <title>{{ feed_title }}</title>
  <updated>{{ updated }}</updated>
  <author><name>inkbook-digest</name></author>
  <link rel="self" href="{{ self_url }}" type="application/atom+xml;profile=opds-catalog;kind=navigation"/>
  <link rel="start" href="{{ self_url }}" type="application/atom+xml;profile=opds-catalog;kind=navigation"/>
  {% for entry in entries %}
  <entry>
    <id>{{ entry.id }}</id>
    <title>{{ entry.title }}</title>
    <updated>{{ updated }}</updated>
    <content type="text">{{ entry.summary }}</content>
    <link rel="subsection" href="{{ entry.href }}" type="application/atom+xml;profile=opds-catalog;kind=acquisition"/>
  </entry>
  {% endfor %}
</feed>
